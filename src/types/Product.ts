type Product = {
    id: string
    name: string
    description: string
    inStock: number
    price: number
    imageUrls: Array<string>
    minimumViewAge: number
    minimumPurchaseAge: number
}

export default Product
